import {createContext} from 'react';

export const StoryblokClientContext = createContext(null);
export const StoryblokBridgeInstanceContext = createContext(null);
export const StoryblokDraftMode = createContext(false);
